-- Add index on canonical_id for efficient cross-source identity lookups.
-- Partial index only covers rows where canonical_id is set (saves space).
CREATE INDEX IF NOT EXISTS idx_mangas_canonical_id ON mangas(canonical_id) WHERE canonical_id IS NOT NULL;
